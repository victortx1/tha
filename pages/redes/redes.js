/**
 * redes.js — página de redes da THA.
 */
import { initCore } from '../../js/main.js';
import { renderSocialCards } from '../../js/social.js';

document.addEventListener('DOMContentLoaded', () => {
  initCore();
  renderSocialCards();
});
